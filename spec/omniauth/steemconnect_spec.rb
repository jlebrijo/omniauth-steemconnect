require "spec_helper"

RSpec.describe Omniauth::Steemconnect do
  it "has a version number" do
    expect(Omniauth::Steemconnect::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
